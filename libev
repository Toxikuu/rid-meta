#!/bin/bash
NAME="libev"
VERS="${NAME}_version"; VERS="${!VERS}"
LINK="http://dist.schmorp.de/$NAME/$NAME-$VERS.tar.gz"
UPST="http://dist.schmorp.de/libev"
SELE="body > table:nth-child(2) > tbody:nth-child(1) > tr:nth-child(5) > td:nth-child(1) > a:nth-child(1)"
DEPS=""


IDIR=$(cat << '~fin.'

cm
rm -vf /usr/include/event.h

~fin.
)

RDIR=$(cat << '~fin.'

rm -vf  /usr/lib/libev.{l,}a \
        /usr/lib/libev.so*

~fin.
)

UDIR=$(cat << '~fin.'
~fin.
)
